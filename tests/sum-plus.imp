// IMP Exmaples
// ============

// In this file several tests are provided for KAT. K commit `e14da0a` should be
// used to kompile the definition and run the examples. Compile the definition with
// `kompile --main-module IMP-ANALYSIS --syntax-module IMP-ANALYSIS imp-kat.k`. Run
// the tests with `bash runtests.sh` in the `tests` directory.

// Looping
// -------

// ### `sum-plus.imp`

// This version of the `sum` proogram has additional statements after the main
// while loop.


int n , s ;

n = 10 ;
while (0 <= n) {
  n = n + -1 ;
  s = s + n ;
}

s = s + 300 ;

